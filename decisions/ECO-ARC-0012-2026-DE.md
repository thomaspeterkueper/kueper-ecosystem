# ECO-ARC-0012-2026-DE — Kontrakomologie / Contracomology: Registrierung einer bestehenden Fassade

Status: accepted
Datum: 2026-07-14
Scope: `contracomology`
Betroffene Repositories: `contracomology`, `kueper-knowledge-graph`, `thomas-kueper.de`

## Kontext

Anders als `mishkenaz.org`, `omnizedenz.org` und `avi-modell` ist
`contracomology` **kein leeres Repository**. Es existiert bereits seit
2026-07-03 (vor `kueper-ecosystem` selbst), umfasst 30 Commits und eine
funktionsfähige Next.js-Anwendung, war aber bislang **nicht in der Registry
erfasst**.

`contracomology` hat sich selbst bereits als reine Frontend-Fassade
architektiert (`README.md`, `docs/ARCHITECTURE.md`,
`docs/MIGRATION-FROM-LEGACY-SITE.md`):

> „Contracomology rendert Inhalte. Knowledge Graph, OTA und kueper.com
> definieren Inhalte."

Es migriert eine eigene ältere Legacy-Seite (`contracomology.org`) in vier
Ebenen: Startseite, Kursmodule, KG-Begriffe (`CON:L1:*`, u. a.
`CON:L1:avi-punkt`, `CON:L1:oem`, `CON:L1:ma-u`, `CON:L1:ma-ta-u` — dieselben
Grundbegriffe wie bei AVI und Mishkenaz) und Dokumente (aus
`SYS:KUEPER:ota` / `SYS:KUEPER:kueper-com`). Es hat bereits 5 offene und 2
erledigte External Tasks in eigener Struktur, davon 4 an die KG-Inbox.

Auf `thomas-kueper.de` existiert zusätzlich eine Content-Collection
`kontrakomologie` mit 18 Essays (Musik-/Literaturanalysen, Grundlagen, eigenes
Werk).

## Entscheidung

### 1. Bestehende Architektur wird bestätigt, nicht neu entworfen

Die von `contracomology` selbst festgelegte Architektur ist mit ECO-ARC-0007
konsistent (Fassade vs. Source of Truth) und wird als verbindlich bestätigt:
`contracomology` rendert; `kueper-knowledge-graph` definiert Begriffe/Relationen
(`CON:L1:*`, `KD:KON:N1`); `overtime-archive.org` und `kueper.com` halten
Volltexte. Diese Entscheidung ändert daran nichts, sondern verankert es im
Ökosystem.

### 2. Verhältnis zu `thomas-kueper.de`

Die 18 Essays unter `src/content/kontrakomologie` auf `thomas-kueper.de` sind
nach dem eigenen Migrationsplan von `contracomology` Kandidaten für die
Dokumentebene (OTA/kueper.com) bzw. für Kursmodule. `thomas-kueper.de` bleibt
bis zur Umsetzung dieses bereits bestehenden Plans faktische Quelle; diese
Entscheidung verpflichtet niemanden zu einer bestimmten Reihenfolge, sondern
bestätigt: **Überblick (thomas-kueper.de) + Vertiefung (verteilt über
contracomology-Fassade, KG-Begriffe, OTA/kueper.com-Dokumente), beide müssen
zusammenpassen** — analog zum Muster aus ECO-ARC-0010/0011, hier auf drei
statt zwei Orte verteilt.

### 3. Rolle, Code, Registrierung

Rolle: `application` (reine Fassade, wie `noxiagame`). Ökosystem-Code:
`CONTRA`.

### 4. Governance-Lücke

`external-tasks/rejected/` fehlt (Pflichtpfad). Wird durch die zentrale Stelle
ergänzt (wie zuvor bei OTA/`thomas-kueper.de`), da rein strukturell und
risikolos — keine inhaltliche Änderung an der bestehenden Arbeit.

## Auswirkungen

- Registry-Eintrag ergänzt; `contracomology` erscheint ab sofort im
  Collector/Dashboard.
- Integration zu `knowledge-graph` (required, wie selbst deklariert) und zu
  `thomas-kueper-de` (Konsistenzbeziehung wie ECO-ARC-0010/0011) eingetragen.
- Die 5 bereits offenen External Tasks von `contracomology` (davon 4 an die
  KG-Inbox) bleiben unangetastet — sie sind Teil des laufenden, eigenen
  Migrationsplans, nicht dieser Entscheidung.

## Nicht entschieden

- Zeitplan der eigenen Migration (liegt bei `contracomology`/Owner).
- Ob/wie die 18 `thomas-kueper.de`-Essays einzeln nach OTA/kueper.com
  überführt werden — folgt dem bestehenden `docs/MIGRATION-FROM-LEGACY-SITE.md`.

## Folgeaufgaben

1. Registry-Eintrag `contracomology`. (erledigt)
2. `external-tasks/rejected/` ergänzen (Governance-Lücke). (erledigt)
