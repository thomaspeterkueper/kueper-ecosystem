# Server-side only. NEVER prefix with NEXT_PUBLIC_.
# GitHub token with read access to the ecosystem repositories.
GH_TOKEN=
# Optional: override the control-plane repo that holds registry/projects.json
# REGISTRY_REPO=thomaspeterkueper/kueper-ecosystem

# Request Trace telemetry. Server-side only; never expose this key to the browser.
# SUPABASE_URL defaults to the KUEPER control-plane project URL when omitted.
SUPABASE_URL=https://ehuoafluxkmizvatmyzt.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
